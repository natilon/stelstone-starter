{
  "name": "stelstone-starter",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Astro site + the Stelstone admin, deployed as one Cloudflare Worker.",
  "scripts": {
    "dev": "node --env-file-if-exists=.env node_modules/.bin/astro dev",
    "build": "astro build && node scripts/postbuild.mjs",
    "preview": "npm run build && wrangler dev -c worker/wrangler.jsonc",
    "deploy": "npm run build && wrangler deploy -c worker/wrangler.jsonc"
  },
  "dependencies": {
    "@stelstone/admin-ui": "^0.29.0",
    "@stelstone/astro-blocks": "^0.12.0",
    "stelstone": "^6.0.7",
    "@stelstone/blocks": "^0.11.0",
    "@stelstone/server": "^0.37.0",
    "@stelstone/worker": "^0.8.9",
    "astro": "^7.0.0"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
